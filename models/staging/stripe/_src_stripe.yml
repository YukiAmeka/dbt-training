sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payments
        config:
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 60
              period: day